version: 2.1
orbs:
  node: circleci/node@5
jobs:
  checkoutEtTests:
    machine: true
    steps:
      - checkout 
      - run: 
          command: |
            cd backend
            npm install 


workflows:
  build: 
    jobs: 
    - checkoutEtTests
