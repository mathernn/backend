version: 2.1
orbs:
  node: circleci/node@5
jobs:
  checkoutEtTests:
    run:
      command:
        git clone git@github.com:mathernn/backend.git
        ls -l



workflows:
  build-and-test:
    jobs:
      - checkoutEtTests